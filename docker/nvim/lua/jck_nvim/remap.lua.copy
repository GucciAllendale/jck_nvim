local which_key = require("which-key")
local mappings = {
    {"<leader>", group="JCK"},
    {"<leader>fe", 
      "<cmd> NvimTreeToggle<cr>", 
      desc="Open nvim-tree",
      group="idek"
    }
}
which_key.add(mappings)
